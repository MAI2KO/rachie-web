# Legacy Apps Script Reference

`rachie-apps-script` is the R.A.C.H.I.E / Whiteout Survival production Google Apps Script snapshot.

`peggie-apps-script` is the P.E.G.G.I.E / Kingshot production Google Apps Script snapshot.

These directories contain migration reference material only. Codex must treat their contents as immutable unless explicitly instructed otherwise.

Production secrets must be redacted before source files are pasted into these directories. These files must never be deployed automatically.
